services:
  # Backend Web Service
  - type: web
    name: kahe-cms-backend-v4
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: kahe-cms-db-v4
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: PYTHON_VERSION
        value: 3.10.0

  # Frontend Static Site
  - type: web
    name: kahe-cms-frontend-v4
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: build
    envVars:
      - key: REACT_APP_API_URL
        fromService:
          type: web
          name: kahe-cms-backend-v4
          property: host
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

databases:
  - name: kahe-cms-db-v4
    plan: free
